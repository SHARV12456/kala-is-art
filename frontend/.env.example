# ============================================================
# KALA IS ART — FRONTEND ENVIRONMENT VARIABLES
# ============================================================
# Set these in Vercel → Project → Settings → Environment Variables
# For local dev, create a .env.local file with these same keys
# NEVER commit .env.local or .env to version control

# ─── REQUIRED ────────────────────────────────────────────────
# The full URL of your Render backend service (no trailing slash)
# Example: https://kala-is-art-backend.onrender.com
VITE_API_URL=https://your-backend.onrender.com/api
